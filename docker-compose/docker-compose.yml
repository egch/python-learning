services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: p123
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data
